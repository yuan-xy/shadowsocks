# Defaults for asshole initscript
# sourced by /etc/init.d/asshole
# installed at /etc/default/asshole by the maintainer scripts

USERID="nobody"

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS="-q -c /etc/asshole/config.json"
